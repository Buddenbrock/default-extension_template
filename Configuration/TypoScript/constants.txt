config {
  # cat=config; type=string; label= Prefix for the website urls. Set this if you are running the site below the root directory
  absRefPrefix = /

  # cat=config; type=string; label= Enable/Disable the admin panel
  adminPanel = 0

  # cat=config; type=string; label= Enable/Disable debug helpers
  debug = 0
}

plugin.meta {
  # cat=plugin.meta; type=string; label= Robots: Use for instance these codes: Index all pages: "index,follow".  Index no pages: "none". Only this page: "index,nofollow".  Only subpages: "noindex,follow"
  robots = index,follow

  # cat=plugin.meta; type=string; label= Reply-to email
  email =

  # cat=plugin.meta; type=string; label= Author: Enter name of author.
  author =

  # The meta tags below are not used on this website, if you want to use these, you can configure that in TypoScript template page.meta

  # cat=plugin.meta; type=boolean; label=Always include global.
  includeGlobal = 0

  # cat=plugin.meta; type=string; label= Distribution
  distribution = GLOBAL

  # cat=plugin.meta; type=options[,General,Mature,14 years,Restricted]; label= Rating
  rating = General
}

styles.content {
  # This defines the maximum width of images inserted in content records of type Images or Text-with-images.
  # There are seperate settings for images floated next to text (..InText)
  imgtext {
    maxW = 1400
    maxWInText = 1400
    borderThick = 1
    linkWrap.newWindow = 1
  }
  uploads {
    jumpurl_secure = 1
    jumpurl_secure_mimeTypes = pdf=application/pdf, doc=application/msword
    jumpurl = 1
  }
}

# customsubcategory=sources=Quellen
# customsubcategory=targets=Ziele

targets {
  pages {
    # cat=XYZ Website/targets/pages; type=int+; label= ID der Startseite
    start =
  }
}
sources {
  menu {
    # cat=XYZ Website/sources/menu; type=int+; label= Hauptnavigation: ID des Ordners
    main =
    # cat=XYZ Website/sources/menu; type=int+; label= Fußnavigation: ID des Ordners
    footer =
    # cat=XYZ Website/sources/menu; type=int+; label= Meta-Navigation: ID des Ordners
    meta =
  }
}

# Add Extension constants here
#<INCLUDE_TYPOSCRIPT: source="FILE:EXT:xyz_template/Configuration/TypoScript/Extensions/News/constants.txt">
